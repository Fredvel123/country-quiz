import React, { Fragment } from 'react'

function CountryCard({ country, flags }) {
  console.log(country );
  return (
    <Fragment>
      <div>
        
      </div>
    </Fragment>
  );
}

export default CountryCard;
